{
  "name": "@phneakngar/ws-do",
  "version": "0.0.150",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --persist-to ../web/.wrangler/state",
    "deploy": "wrangler deploy",
    "knip": "knip",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit -p tsconfig.build.json",
    "lint": "eslint src --config ../../eslint.workers.config.mjs"
  },
  "dependencies": {
    "@phneakngar/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260621.1",
    "@types/node": "^25.9.4",
    "knip": "^6.17.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.103.0"
  }
}
