{
  "name": "@phneakngar/ws-do",
  "version": "0.0.6",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --persist-to ../web/.wrangler/state --inspector-port 0",
    "deploy": "wrangler deploy",
    "knip": "knip",
    "test": "vitest run -- --passWithNoTests",
    "typecheck": "tsc --noEmit -p tsconfig.build.json",
    "lint": "eslint src --config ../../eslint.workers.config.mjs"
  },
  "dependencies": {
    "@phneakngar/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260812.1",
    "@types/node": "^25.9.5",
    "knip": "^6.32.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.122.0"
  }
}
